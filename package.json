{
  "name": "treino-powerlifting",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "PWA de powerlifting com treino offline, planos versionados e agente remoto opcional.",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "test": "node --test test/*.test.js",
    "test:e2e": "playwright test",
    "check": "node scripts/check.js"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.28.2",
    "fake-indexeddb": "^6.2.5"
  }
}
